# Radius Estimation for Census Data

Quickly set a coordinate point, define a distance around it, clip tracts, estimate tract space, get census attributes, estimate a total for the attributes in radius, and output a csv.

Uses Nominatim as a geocoder.

Currently, does not rename attributes, and is set up to pull the entire table. 
